# @dsh/plugin-desktop-bridge bundle patch: registers this plugin into a profile's layer
# stack when installed via the official CLI:
#   dsh plugin --profile web add @dsh/plugin-desktop-bridge
# The dsh CLI reconciles this declaration into dsh.profile.bundles.
- insert:
    - id: desktop-bridge
      name: "@dsh/plugin-desktop-bridge"