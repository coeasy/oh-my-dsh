# @dsh/plugin-usage-analytics bundle patch: registers this plugin into a profile's layer
# stack when installed via the official CLI:
#   dsh plugin --profile web add @dsh/plugin-usage-analytics
# The dsh CLI reconciles this declaration into dsh.profile.bundles.
- insert:
    - id: usage-analytics
      name: "@dsh/plugin-usage-analytics"
