{
  "name": "@dsh/plugin-model-config",
  "version": "0.1.0-build.fa7b81b5",
  "private": true,
  "type": "module",
  "description": "Cordis bundle: per-stage model routing (default/planning/subagent/evaluation), reasoning effort, and Profiles.",
  "main": "./out/index.js",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./out/index.js"
    }
  },
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --format=esm --outfile=out/index.js && esbuild ui-src/mount.ts --bundle --platform=browser --format=iife --outfile=ui/bundle.js",
    "test": "node --experimental-strip-types --test \"tests/*.test.ts\""
  },
  "license": "MIT",
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    }
  }
}
