# @dsh/plugin-degeneration-guard bundle patch: registers this plugin into a profile's layer
# stack when installed via the official CLI:
#   dsh plugin --profile web add @dsh/plugin-degeneration-guard
# The dsh CLI reconciles this declaration into dsh.profile.bundles.
- insert:
    - id: degeneration-guard
      name: "@dsh/plugin-degeneration-guard"
